import React from "react";
import styles from "./ButlerContainer.mod.css";

const ButlerContainer = props => (
  <div className={styles().container}>I am ButlerContainer</div>
);

export default ButlerContainer;
